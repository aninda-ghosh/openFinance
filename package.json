{
  "name": "openfinance",
  "version": "3.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "biome check .",
    "tauri:dev": "pnpm --filter openfinance-desktop tauri dev"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "turbo": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3-multiple-ciphers",
      "esbuild"
    ]
  }
}
