// Migrations are applied automatically on server startup via inline CREATE TABLE IF NOT EXISTS
// statements in src/index.ts. This file is kept as a no-op placeholder.
console.log(
  "No manual migration step needed — schema is applied on server startup."
);
